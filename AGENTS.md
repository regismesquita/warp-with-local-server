# Agent Notes

- This checkout is for personal work. Do not create, update, or require Linear tickets unless explicitly requested.
- GitHub Actions are intentionally removed locally. When merging from `upstream/master`, preserve local deletions under `.github/workflows/` unless the user explicitly asks to restore them.
- If upstream modifies GitHub workflow files that are deleted locally, resolve the modify/delete conflict in favor of keeping the files deleted.
